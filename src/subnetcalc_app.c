#include "flipper.h"
#include "subnetcalc_app.h"

const char* TAG = "SubnetCalc";

/** entrypoint of SubnetCalc */
int32_t subnetcalc_app(void* p) {
    UNUSED(p);
    FURI_LOG_I(TAG, "Starting SubnetCalc App...");

    // TODO: Init the app down here

    // TODO: Clean up
}